# Week 7 D2 — B2H One-Page Summary

โครงการ B2H เป็นเว็บแอปสำหรับซื้อ-ขายของมือสองภายในชุมชนมหาวิทยาลัย เน้นความง่ายและปลอดภัยด้วยการยืนยันตัวตนผ่านอีเมลโดเมนมหาวิทยาลัย พร้อมฟีเจอร์โปรไฟล์ผู้ขาย รายการสินค้า รูปภาพ รีวิว รายการโปรด และรายงานการละเมิด

## Tech Stack
- Frontend: HTML5, CSS3, Vanilla JavaScript
- Backend: Node.js + Express
- Database: MySQL (ผ่าน `mysql2`)
- Auth: JWT (Bearer Token)
- File Upload: Multer (บันทึกลงโฟลเดอร์ `public/uploads`)

## Frontend Pages
- `public/index.html`: หน้าเริ่มต้น/ทางเข้า
- `public/dashboard.html`: หน้ารวมสินค้าและนำทาง
- `public/seller.html`: หน้าข้อมูลผู้ขาย + จัดการโปรไฟล์
- `public/interested.html`: รายการที่สนใจ/ติดดาว

## API Overview (หลัก ๆ)
- `POST /auth/university`: เข้าสู่ระบบด้วยอีเมลโดเมนมหาวิทยาลัย (เช่น `go.buu.ac.th`)
- `POST /register`: สมัครใช้งานด้วยอีเมล + รหัสผ่าน (ตรวจโดเมนมหาวิทยาลัย)
- `POST /login`: เข้าสู่ระบบด้วยอีเมล + รหัสผ่าน, คืน JWT
- `GET /me`: ตรวจสถานะเข้าสู่ระบบและดึงข้อมูลผู้ใช้ปัจจุบัน
- `PUT /me`: อัปเดตโปรไฟล์ผู้ใช้ (ชื่อ, รูป, ช่องทางติดต่อ)
- `POST /logout`: ออกจากระบบแบบ stateless (ฝั่ง client ลบ token)
- `GET /products`: ดึงรายการสินค้า (แสดงสถานะสินค้าเป็น badge ที่ฝั่ง client)
- `GET /favorites`: ดึงรายการโปรดของผู้ใช้ (ต้อง JWT)
- `POST /favorites/toggle`: สลับเพิ่ม/ลบสินค้าในรายการโปรด (ต้อง JWT)

## Database Schema (ย่อ)
ดูรายละเอียดเต็มใน [schema.sql](../schema.sql)

- ตาราง `users`: จัดเก็บผู้ใช้, บทบาท (`user|admin`), สถานะ, โปรไฟล์, ช่องทางติดต่อ
- ตาราง `product`: รายการสินค้า, ผู้ขาย (`seller_id`), ราคา, หมวดหมู่, สถานะ (`available|sold|hidden`)
- ตาราง `product_images`: รูปแนบกับสินค้า (หลายรูปต่อสินค้า)
- ตาราง `reviews`: รีวิวให้กับผู้ขาย/สินค้า, คะแนน 1–5
- ตาราง `favorites`: รายการโปรดของผู้ใช้ต่อสินค้า (unique ต่อ `(user_id, product_id)`)
- ตาราง `report`: รายงานการละเมิด (สถานะ `pending|reviewed|rejected`)

## Architecture
- Express เสิร์ฟไฟล์ static จากโฟลเดอร์ `public`
- Auth ด้วย JWT: แนบ `Authorization: Bearer <token>` ไปกับ API ที่ต้องการสิทธิ์
- เชื่อมต่อฐานข้อมูลผ่าน MySQL Pool (`mysql2/promise`)
- อัปโหลดรูปด้วย Multer เก็บไฟล์ลง `public/uploads`

## Setup & Run (Windows)
1) ติดตั้ง Node.js LTS และ MySQL (เช่น XAMPP/MySQL Server)
2) สร้างฐานข้อมูลด้วยสคริปต์: เปิด MySQL แล้วรัน [schema.sql](../schema.sql)
3) ติดตั้งแพ็กเกจ

```powershell
npm install
```

4) ตั้งค่าการเชื่อมต่อฐานข้อมูล (ดีฟอลต์ใน `server.js` คือ `root` ไม่มีรหัสผ่าน, ฐานข้อมูล `b2h`) หากแตกต่างให้ปรับค่าก่อนรัน
5) เริ่มเซิร์ฟเวอร์

```powershell
npm start
```

เปิดเบราว์เซอร์ไปที่ `http://localhost:3000` (หรือพอร์ตตามที่ตั้งค่า)

## Key Notes / ข้อสังเกต
- JWT Secret ถูกกำหนดในโค้ด (เพื่อเดโม) ควรย้ายไป `.env` ในโปรดักชัน
- ตรวจโดเมนอีเมลมหาวิทยาลัยผ่านตัวแปร `UNIVERSITY_DOMAINS` (ค่าเริ่มต้น `go.buu.ac.th`)
- ใช้ `multer` สำหรับอัปโหลดรูป เก็บไฟล์ในตัวเครื่อง ไม่ใช่ object storage
- มีการอ้างอิงแพ็กเกจ `express-session` ใน `package.json` แต่ไม่ได้ใช้ใน `server.js` (สามารถลบออกหรือใช้งานตามความเหมาะสม)

## Next Steps
- เพิ่มการจัดการสินค้า: สร้าง/แก้ไข/ลบ (CRUD) พร้อมการตรวจสิทธิ์ผู้ขาย
- ย้าย secret และคอนฟิกฐานข้อมูลไป `.env`
- เพิ่ม pagination/filter/search ให้กับ `/products`
- ปรับปรุงหน้า `interested.html` ให้เชื่อม `/favorites` เต็มรูปแบบ
- พิจารณาใช้ cloud storage สำหรับรูปภาพและเพิ่มขนาดไฟล์สูงสุด

## File Organization
### JavaScript Structure

```
src/
├── controllers/
│   ├── authController.js
│   └──roductController.js
│ 
├── models/
│   └──db.js                
│ 
├── routes/
│   ├── authRoutes.js        
│   └──userRoutes.js        
├── public/
│   ├── dashboard.html
│   ├── index.html
│   ├── dashboard.html
│   ├── interested.html
│   └── seller.html
└── server.js

## Error Handling
### Custom Error Messages

เป้าหมาย
- ตรวจสอบข้อมูลฝั่งผู้ใช้ก่อนส่ง (client-side validation) เพื่อลดรอบเรียก API
- ให้รูปแบบข้อผิดพลาดจาก API สม่ำเสมอและเข้าใจง่าย
- มี global error handler ใน Express เพื่อจัดการข้อความและสถานะ HTTP เดียวกันทั้งระบบ

Frontend ตัวอย่าง (ตรวจสอบฟอร์มและแจ้งเตือน)

```javascript
function displayErrorNotification(message) {
	// ตัวอย่างอย่างง่าย: แสดงข้อความที่มุมจอหรือในพื้นที่แจ้งเตือน
	const box = document.getElementById('error-box');
	if (box) { box.textContent = message; box.style.display = 'block'; }
	alert(message); // หรือใช้ toast/notification ของ UI framework
}

async function handleCreateProductSubmit(event) {
	event.preventDefault();
	const title = document.querySelector('#title').value.trim();
	const price = Number(document.querySelector('#price').value);

	if (!title) {
		return displayErrorNotification('กรุณากรอกชื่อสินค้า');
	}
	if (!price || price <= 0) {
		return displayErrorNotification('กรุณาระบุราคาให้ถูกต้อง');
	}

	try {
		const token = window.localStorage.getItem('token');
		const res = await fetch('/products', {
			method: 'POST',
			headers: {
				'Content-Type': 'application/json',
				Authorization: token ? `Bearer ${token}` : undefined,
			},
			body: JSON.stringify({ title, price }),
		});
		const data = await res.json();
		if (!res.ok || !data.success) {
			throw new Error(data.message || 'เกิดข้อผิดพลาด');
		}
		// สำเร็จ: เคลียร์ฟอร์ม/รีเฟรชรายการสินค้า
	} catch (err) {
		console.error('Submit error:', err.message);
		displayErrorNotification(err.message);
	}
}
```

Backend ตัวอย่าง (ใช้คลาส error และ global error handler)

```javascript
class ValidationError extends Error {
	constructor(message, status = 400) {
		super(message);
		this.name = 'ValidationError';
		this.status = status;
	}
}

app.post('/products', authenticateJWT, async (req, res, next) => {
	try {
		const { title, price } = req.body || {};
		if (!title) throw new ValidationError('กรุณากรอกชื่อสินค้า');
		if (!price || Number(price) <= 0)
			throw new ValidationError('กรุณาระบุราคาให้ถูกต้อง');

		// TODO: บันทึกลงฐานข้อมูล แล้วคืนผลลัพธ์
		return res.json({ success: true, message: 'เพิ่มสินค้าเรียบร้อย' });
	} catch (err) {
		next(err);
	}
});

// Global error handler (วางหลัง routes ทั้งหมด)
app.use((err, req, res, next) => {
	const status = err.status || 500;
	const message = err.message || 'เกิดข้อผิดพลาดจากเซิร์ฟเวอร์';
	res.status(status).json({ success: false, message });
});
```
## 8. Code Review Checklist

### การตั้งชื่อและโครงสร้าง

- [ ] ปฏิบัติตามมาตรฐานการตั้งชื่อ (Naming Conventions)
- [ ] ไม่มีค่าที่ hardcoded (ใช้ constants แทน)
- [ ] ไม่มี magic numbers (0, 1, 100 ที่ไม่มีความหมาย)
- [ ] โครงสร้างโฟลเดอร์เป็นระเบียบ

### Error Handling & Logging

- [x] มีการ handle errors อย่างเหมาะสม
- [ ] มีคำอธิบายสำหรับ logic ที่ซับซ้อน
- [ ] ไม่มี console.log() ในโค้ด production
- [x] มี logging สำหรับ errors ที่สำคัญ

### Data & API

- [ ] ไม่มี data hardcoded (ใช้ API/database แทน)
- [ ] ไม่มี credentials/secrets ใน code
- [x] Input validation present

### UI/UX & Accessibility

- [ ] ทดสอบการออกแบบแบบ responsive (mobile, tablet, desktop)
- [ ] มี ARIA labels สำหรับผู้พิการ
- [ ] Keyboard navigation ทำงานได้
- [ ] Focus states ชัดเจน

### Performance & Optimization

- [ ] มี debouncing/throttling ที่จำเป็น
- [ ] ไม่มี N+1 queries
- [ ] Performance optimized (caching, lazy loading)

### Security

- [x] ตรวจสอบ input validation
- [ ] ป้องกัน XSS (Cross-Site Scripting)
- [x] ป้องกัน SQL injection
- [ ] No exposed secrets/API keys

✅ **Checklist ส่วนนี้:**
- [ ] Coding_Standards.md เขียนเสร็จ
- [ ] HTML, CSS, JS naming standards ชัดเจน
- [x] File organization structure กำหนด

### ขั้นตอนที่ 2️⃣: ตั้งค่า Git Repository

#### 2.1 สร้าง .gitignore

```
# .gitignore

# Node.js

node_modules/
npm-debug.log
yarn-error.log
package-lock.json

# Database

_.db
_.sqlite
\*.sqlite3

# Environment

.env
.env.local
.env.\*.local

# IDE

.vscode/
.idea/
_.swp
_.swo
_.sublime-project
_.sublime-workspace

# OS

.DS_Store
Thumbs.db

# Dist & Build

dist/
build/
_.min.js
_.min.css

# Logs

logs/
\*.log

```
#### 2.2 สร้าง README.md (B2H)

```markdown
# B2H Marketplace

A marketplace for the university community built with HTML5, CSS3, JavaScript, Node.js, Express, and MySQL.

## Features

- ✅ University email sign-in (domain check)
- ✅ JWT-based authentication
- ✅ View products and seller profiles
- ✅ Upload product images
- ✅ Favorites list
- ✅ Reviews and ratings
- ✅ Report abuse
- ✅ Responsive UI

## Tech Stack

- **Frontend:** HTML5, CSS3, Vanilla JavaScript
- **Backend:** Node.js + Express
- **Database:** MySQL (via `mysql2`)
- **Tools:** Nodemon (dev), Multer (uploads), Jest (optional testing)

## Prerequisites

- Node.js 18+
- MySQL 8+
- npm or yarn

## Installation

1. Install dependencies

```
npm install
```

2. Setup database (run the schema)

```
# In MySQL client
source schema.sql;
```

3. Configure environment (recommended)

Create `.env`:

```
JWT_SECRET=change-me
UNIVERSITY_DOMAINS=go.buu.ac.th
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=b2h
DB_PORT=3306
```

4. Start development server

```
npm start
```

Application runs at: http://localhost:3000

## Project Structure (current)

```
public/
	dashboard.html
	index.html
	interested.html
	seller.html
	uploads/
server.js
package.json
schema.sql
```

## API Endpoints (core)

- POST /auth/university
- POST /register
- POST /login
- GET /me
- PUT /me
- GET /products
- GET /favorites
- POST /favorites/toggle

## Development

- Run tests (if added): `npm test`
- Lint/format (if configured): `npm run lint`, `npm run format`

## Contributing

- Follow coding standards
- Write tests for new features
- Update documentation

## License

MIT License
```
✅ **Checklist ส่วนนี้:**
- [ ] .gitignore ตั้งค่า
- [ ] README.md เขียน
- [ ] PR template สร้าง
- [ ] GitHub Actions CI ตั้งค่า
---

### ขั้นตอนที่ 3️⃣: สร้าง Code Skeleton

#### 3.1 package.json (ตัวอย่าง)
```
{
  "name": "simple-login",
  "version": "1.0.0",
  "description": "Minimal login example with Express and SQLite",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "init-db": "node init_db.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.16.3"
  }
}
```
```
#### 3.3 public/dashboard.html
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>B2H — Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: #f8f9fa
        }
    </style>
    <style>
        .fav-icon { width:18px;height:18px;vertical-align:middle;margin-right:6px }
        .fav-on { fill:#e74c3c; stroke:#e74c3c }
        .fav-off { fill:#ffffff; stroke:#e74c3c }
        @media (max-width:576px) { .fav-text { display:none } }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="#">B2H</a>
            <div class="d-flex align-items-center">
                <img id="navAvatar" src="" alt="Avatar" class="rounded-circle" title="ดูโปรไฟล์"
                    style="width:32px;height:32px;object-fit:cover;display:none;cursor:pointer;">
                <span id="navUser" class="ms-2 me-3 text-muted" style="cursor:pointer" title="ดูโปรไฟล์"></span>
                <button id="favoritesBtn" class="btn btn-outline-secondary btn-sm me-2" title="รายการที่ชอบ">❤ รายการที่ชอบ</button>
                <button id="logout" class="btn btn-outline-secondary btn-sm">Logout</button>
            </div>
        </div>
    </nav>
</body>

#### 3.5 B2H/app.js (ตัวอย่าง)
```javascript
const express = require('express');
const bodyParser = require('body-parser');
const path = require('path');

const app = express();

// Middlewares
app.use(bodyParser.json());
app.use(express.static(path.join(__dirname, 'public')));

// Frontend pages
app.get('/dashboard', (req, res) => {
  res.sendFile(path.join(__dirname, 'public', 'dashboard.html'));
});
app.get('/seller/:id', (req, res) => {
  const id = req.params.id;
  return res.redirect(`/seller.html?seller_id=${encodeURIComponent(id)}`);
});
app.get('/seller', (req, res) => {
  res.sendFile(path.join(__dirname, 'public', 'seller.html'));
});

// Routes (MVC)
// const authRoutes = require('./routes/authRoutes');
// const productRoutes = require('./routes/productRoutes');
// app.use('/', authRoutes);
// app.use('/', productRoutes);

// Diagnostics
app.get('/_health', (req, res) => res.json({ ok: true, uptime: process.uptime() }));
app.get('/_routes', (req, res) => {
  try {
    const routes = [];
    if (app && app._router && Array.isArray(app._router.stack)) {
      app._router.stack.forEach((layer) => {
        if (layer.route && layer.route.path) {
          routes.push({ path: layer.route.path, methods: Object.keys(layer.route.methods) });
        }
      });
    }
    res.json({ success: true, routes });
  } catch (e) {
    res.status(500).json({ success: false, message: 'Unable to enumerate routes' });
  }
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log(`Server running on http://localhost:${PORT}`));
```
```
ภารกิจ 7.2: Sprint Planning + Development Setup
### ขั้นตอนที่ 1️⃣: Sprint 1 Backlog
Sprint Information

- **Start Date:** 2025-12-15
- **End Date:** 2026-03-23
- **Duration:** 15 weeks (45 working days)
- **Team Size:** Product Owner 1, Scrum Master 1, 2 developers, QA/DevOps 2
- **Estimated Capacity:** 15 story points

## Sprint Goal
B2H เป็นแพลตฟอร์มเว็บที่ออกแบบมาเพื่อให้นิสิตหรือบุคลากรมหาวิทยาลัยบูรพาสามารถซื้อ–ขายของมือสองภายในมหาวิทยาลัยได้อย่างปลอดภัย เช่น หนังสือเรียน อุปกรณ์การเรียน และของใช้ส่วนตัว โดยมุ่งเน้นความน่าเชื่อถือและความสะดวกในการใช้งาน
```
```
## Selected Stories (30 points)

### Story 1: Login (5 points)
**Priority:** High | **Assigned:** Dev A
ในฐานะ: ผู้ใช้
ฉันต้องการ: เข้าสู่ระบบด้วยอีเมลและรหัสผ่าน
เพื่อที่ว่า: สามารถใช้งานระบบได้

เกณฑ์การยอมรับ :
-ระบบตรวจสอบรหัสผ่านถูกต้อง
-แสดงข้อผิดพลาดเมื่อข้อมูลไม่ถูก
-เข้าสู่ระบบสำเร็จเมื่อข้อมูลถูกต้อง
**Tasks:**
- Task 1.1: Create index.HTML (2h) - Dev A
- Task 1.2: Implement login functions (3h) - Dev A
- Task 1.3: Add localStorage persistence (2h) - Dev A

### Story 2: Edit Profile (3 points)
**Priority:** High | **Assigned:** Dev A
ในฐานะ: ผู้ใช้
ฉันต้องการ: แก้ไขข้อมูลส่วนตัว เช่น รูปภาพ / LINE / Facebook
เพื่อที่ว่า: ผู้ซื้อหรือผู้ขายสามารถติดต่อกันได้

เกณฑ์การยอมรับ :
-ผู้ใช้แก้ไขชื่อ รูป ช่องทางติดต่อได้
-ระบบบันทึกข้อมูลใหม่ได้ถูกต้อง
**Tasks:**
- Task 1.1: Create dashboard.HTML (3h) - Dev A
- Task 1.2: Implement Edit functions (3h) - Dev A
- Task 1.3: Add localStorage persistence (2h) - Dev A

### Story 2: Filter Products (3 points)
**Priority:** medium | **Assigned:** Dev B
ในฐานะ: ผู้ใช้
ฉันต้องการ: คัดกรองสินค้าตามหมวดหมู่หรือราคา
เพื่อที่ว่า: ได้สินค้าที่ตรงกับความต้องการ

เกณฑ์การยอมรับ :
-เลือกหมวดหมู่ เช่น หนังสือ, อุปกรณ์ไอที
-กรองตามช่วงราคาได้
**Tasks:**
- Task 2.1: Create filter UI (1h) - Dev B
- Task 2.2: Implement filter logic (2h) - Dev B
- Task 2.3: Add statistics display (1.5h) - Dev B

## Sprint Schedule
Week 1:
- Tuesday: Sprint Planning, setup development environment
- Wednesday: Core feature development (CRUD)
- Thursday: ProjectCharter, Userstory, Srs

Week 2:
- Tuesday: Filter feature and database integration
- Wednesday: UI Polish and accessibility
- Thursday:  Code review, demo, and retrospective

## Daily Standup
- **Time:** 1:00 PM (15 minutes)
- **Format:** Video call (Zoom/Teams)
- **Attendees:** All team members
- **Questions:**
  1. What did I do yesterday?
  2. What will I do today?
  3. Any blockers?

## Code Review Process
- **When:** 1:00 PM - 2:00 PM
- **Requirement:** 2+ approvals to merge
- **Turnaround:** 24 hours max

## Risk Register

| Risk | Probability | Impact | Mitigation |
|------|-------------|--------|-----------|
| Database integration takes longer | Medium | High | Mock API ready, pair programming |
| API responses slow | Low | Medium | Load testing planned, optimize queries |
| Team member sick | Low | High | Cross-training on all features |
| Scope creep from PO | High | High | SM protects scope, change control |

## Success Criteria

- ✅ All 5 stories completed
-     30 story points delivered
-     Code coverage > 80%
-     Zero critical bugs
- ✅ Team satisfied with pace
- ✅ Ready for demo
```
```
### ขั้นตอนที่ 2️⃣: Database Setup
#### database/schema.sql
-- ===============================
-- DATABASE
-- ===============================
CREATE DATABASE IF NOT EXISTS b2h
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE b2h;

-- ===============================
-- USERS (เฉพาะ user / admin)
-- ===============================
CREATE TABLE users (
  user_id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  email VARCHAR(100) NOT NULL UNIQUE,
  role ENUM('user','admin') NOT NULL DEFAULT 'user',
  status ENUM('active','banned') NOT NULL DEFAULT 'active',
  profile_image VARCHAR(255),
  contact_facebook VARCHAR(255),
  contact_line VARCHAR(255),
  contact_instagram VARCHAR(255),
  create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB;

-- ===============================
-- PRODUCT
-- ===============================
CREATE TABLE product (
  product_id INT AUTO_INCREMENT PRIMARY KEY,
  seller_id INT NOT NULL,
  title VARCHAR(100) NOT NULL,
  description LONGTEXT,
  price DECIMAL(10,2) NOT NULL,
  contact VARCHAR(255),
  category VARCHAR(100),
  status ENUM('available','sold','hidden') DEFAULT 'available',
  create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  CONSTRAINT fk_product_seller
    FOREIGN KEY (seller_id)
    REFERENCES users(user_id)
    ON DELETE CASCADE
) ENGINE=InnoDB;

-- ===============================
-- PRODUCT IMAGES
-- ===============================
CREATE TABLE product_images (
  image_id INT AUTO_INCREMENT PRIMARY KEY,
  product_id INT NOT NULL,
  img_url VARCHAR(255) NOT NULL,

  CONSTRAINT fk_product_images_product
    FOREIGN KEY (product_id)
    REFERENCES product(product_id)
    ON DELETE CASCADE
) ENGINE=InnoDB;

-- ===============================
-- REVIEWS
-- ===============================
CREATE TABLE reviews (
  review_id INT AUTO_INCREMENT PRIMARY KEY,
  reviewer_id INT NOT NULL,
  seller_id INT NOT NULL,
  product_id INT NOT NULL,
  rating INT NOT NULL,
  comment LONGTEXT,
  create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  CONSTRAINT fk_reviews_reviewer
    FOREIGN KEY (reviewer_id)
    REFERENCES users(user_id)
    ON DELETE CASCADE,

  CONSTRAINT fk_reviews_seller
    FOREIGN KEY (seller_id)
    REFERENCES users(user_id)
    ON DELETE CASCADE,

  CONSTRAINT fk_reviews_product
    FOREIGN KEY (product_id)
    REFERENCES product(product_id)
    ON DELETE CASCADE,

  CONSTRAINT chk_rating
    CHECK (rating BETWEEN 1 AND 5)
) ENGINE=InnoDB;

-- ===============================
-- FAVORITES
-- ===============================
CREATE TABLE favorites (
  favorite_id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  product_id INT NOT NULL,

  CONSTRAINT fk_favorites_user
    FOREIGN KEY (user_id)
    REFERENCES users(user_id)
    ON DELETE CASCADE,

  CONSTRAINT fk_favorites_product
    FOREIGN KEY (product_id)
    REFERENCES product(product_id)
    ON DELETE CASCADE,

  UNIQUE KEY uk_user_product (user_id, product_id)
) ENGINE=InnoDB;

-- ===============================
-- REPORT
-- ===============================
CREATE TABLE report (
  report_id INT AUTO_INCREMENT PRIMARY KEY,
  reporter_id INT NOT NULL,
  target_type ENUM('user','product','review') NOT NULL,
  target_id INT NOT NULL,
  reason LONGTEXT NOT NULL,
  status ENUM('pending','reviewed','rejected') DEFAULT 'pending',
  create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  CONSTRAINT fk_report_reporter
    FOREIGN KEY (reporter_id)
    REFERENCES users(user_id)
    ON DELETE CASCADE
) ENGINE=InnoDB;
```
---

### ขั้นตอนที่ 3️⃣: Development Environment Setup

#### docs/DEVELOPMENT.md

````markdown
# Development Environment Setup

## System Requirements

- Node.js 14+
- npm 6+ or yarn 1.22+
- SQLite3
- Git 2.30+
- Text editor (VS Code, Sublime, etc.)

## Installation Steps

### 1. Clone Repository

```bash
git clone https://github.com/yourname/todo-app.git
cd todo-app
```
````

### 2. Install Dependencies

```bash
npm install
```

### 3. Setup Database

```bash

# Initialize database schema

npm run db:init

# (Optional) Seed with sample data

npm run db:seed
```

### 4. Create Environment File

```bash
cp .env.example .env
```

Edit .env if needed:

```
NODE_ENV=development
PORT=3000
DB_PATH=database.db
```

### 5. Start Development Server

```bash
npm run dev
```

Open browser: http://localhost:3000

## Verification

- [/] Server running at http://localhost:3000
- [ ] Health check: http://localhost:3000/api/health → {"status":"UP"}
- [ ] Database file exists: \`database.db\`
- [ ] Can create/read/update/delete todos
- [ ] No console errors

## 🎯 ภารกิจ 7.3: Code Review Training
### Code_Review_Checklist.md
```markdown
# Code Review Checklist - B2H 
## Before Starting Review

- [ ] Understand the feature/fix
- [ ] Read related issues
- [ ] Understand acceptance criteria
- [ ] Know the code style guide

## Functionality (Must Have)

- [ ] Code does what it's supposed to do
- [ ] All acceptance criteria met
- [ ] Edge cases handled
  - Empty title/description
  - Maximum length exceeded
  - Database errors
  - Network errors
  - No todos exist
 - [x] Error messages are clear

## Code Quality (Must Have)

- [ ] Follows Coding Standards
- [ ] Variable/function names are clear
- [ ] No duplicate code
- [ ] Functions are focused (single responsibility)
- [ ] Methods not too long (< 30 lines ideal)
- [ ] Comments explain "why", not "what"
- [ ] No console.log() in production code
- [ ] No hardcoded values (use constants)
- [ ] No TODO comments left behind

## Testing (Must Have)

- [ ] Unit tests added for logic
- [ ] Integration tests for API
- [ ] Happy path tested
- [ ] Error scenarios tested
- [ ] Test names are clear
- [ ] No skipped tests
- [ ] Code coverage reasonable (>80%)

## Performance (Should Have)

- [ ] No N+1 database queries
- [ ] Debouncing on search/input
- [ ] No memory leaks
- [ ] Efficient data structures
- [ ] Database queries optimized

## Security (Must Have)

 - [ ] No hardcoded secrets
 - [x] Input validation present
 - [x] SQL injection prevented (parameterized queries)
- [ ] XSS prevention (sanitized output)
- [ ] Authentication/authorization correct

## Accessibility (Should Have)

- [ ] ARIA labels present
- [ ] Keyboard navigation works
- [ ] Color contrast sufficient
- [ ] Form labels associated
- [ ] Focus states visible

## Documentation (Should Have)

- [ ] Code comments for complex logic
- [ ] JSDoc comments for functions
 - [ ] README updated if needed
 - [x] API documentation updated

## UI/UX (For Frontend)

- [ ] Responsive design (mobile, tablet, desktop)
- [ ] Touch-friendly (48px buttons)
- [ ] Smooth transitions
- [ ] Loading states shown
- [ ] Error states shown
- [ ] Empty states handled

## Checklist for Database Changes

- [ ] Schema changes backward compatible
- [ ] Migrations written
- [ ] Indexes added where needed
- [ ] No N+1 queries

## ✅ Deliverables Summary

```
wk07- B2H

### ภารกิจ 7.1 :
[ ] Coding_Standards.md - HTML, CSS, JS standards with examples
[ ] .gitignore - Complete gitignore for Node/frontend projects
[ ] README.md - Full project documentation
[ ] PR template - Structured pull request template
[ ] GitHub Actions CI - Automated testing pipeline
[ ] Code Skeleton - Complete project structure
  - package.json - Dependencies and scripts
  - index.html - Full HTML with form, list, filters
  - server.js - Complete JavaScript app (400+ lines)

### ภารกิจ 7.2 :
[ ] Sprint_1_Backlog.md
  - 5 user stories (30 points total)
  - Detailed acceptance criteria
  - Task breakdown for each story
  - Sprint schedule and team roles
  - Risk register

[ ] Database setup
  - schema.sql - SQLite database design
  - server.js - Express API (600+ lines)

[ ] DEVELOPMENT.md
  - Step-by-step setup instructions
  - Troubleshooting guide
  - IDE configuration
  - Testing & debugging guides
  - API testing examples

### ภารกิจ 7.3 :
[ ] Code_Review_Checklist.md - Comprehensive review guidelines
```